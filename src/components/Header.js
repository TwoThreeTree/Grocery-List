import React from "react";

const Header = ({ title }) => {
  return (
    <header>
      <h1> {title}</h1>
      <nav>
        <button> click</button>
      </nav>
    </header>
  );
};

export default Header;
